"use strict";

import React from 'react';

import Layout from '../partial/Layout.react'

var Compatibility = React.createClass({
  render: function() {
    return (
      <Layout page="compatibility" />
    )
  }
});

module.exports = Compatibility;
